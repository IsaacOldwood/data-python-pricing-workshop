[project]
name = "data-python-pricing-workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pandas>=3.0.5"]

[tool.uv]
exclude-newer = "5 days"

[dependency-groups]
dev = [
    "matplotlib>=3.11.1",
    "seaborn>=0.13.2",
]
